name: Deploy das alterações para PRD
on:
  pull_request:
    types: [closed]
    branches:
      - master
    paths:
      - 'force-app/**'
      - 'manifest/package.xml' 

jobs:
  deploy_job:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Autenticar no Salesforce
        run: echo ${{ secrets.AUTH_URL_PRD }} > sfdx-auth-url.txt

      - name: Conectar ao Salesforce
        run: sf org login sfdx-url -f sfdx-auth-url.txt --alias prd

      - name: Executar Deploy na PRD
        run: sf project deploy start --test-level NoTestRun -x manifest/package.xml -o prd

  close_job:
    if: github.event.pull_request.merged == false
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "PR #${{ github.event.number }} foi fechado sem realizar o merge."
